apiVersion: v1
kind: Service
metadata:
  name: async-queue-api-nodeport
  namespace: async-queue
spec:
  type: NodePort
  selector:
    app: async-queue-api
  ports:
    - port: 3000
      targetPort: 3000
