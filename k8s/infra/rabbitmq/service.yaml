apiVersion: v1
kind: Service
metadata:
  name: rabbit-service
  namespace: message-broker
spec:
  type: NodePort
  selector:
    app: rabbit-broker
  ports:
    - port: 5672
      targetPort: 5672
    - port: 15672
      targetPort: 15672
